// Package jobtype contains string constants for job type values.
package jobtype

const (
	// Claude indicates the job runs a Claude CLI session with a predefined prompt.
	Claude = "claude"

	// Bash indicates the job runs a bash/shell script.
	Bash = "bash"
)
