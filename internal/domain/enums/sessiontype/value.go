// Package sessiontype contains string constants for session type values.
package sessiontype

const (
	// Claude indicates a Claude CLI session.
	Claude = "claude"

	// Terminal indicates a raw terminal/shell session.
	Terminal = "terminal"
)
