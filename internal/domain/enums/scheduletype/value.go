// Package scheduletype contains string constants for schedule type values.
package scheduletype

const (
	// Recurring indicates the job runs on a repeating schedule.
	Recurring = "recurring"

	// OneTime indicates the job runs once at a specified time.
	OneTime = "one_time"
)
